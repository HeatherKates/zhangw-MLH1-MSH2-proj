process {
  executor = "slurm"
  clusterOptions = "--account=cancercenter-dept --qos=cancercenter-dept-b"
  errorStrategy = {
    task.attempt <= 3 ? "retry" : "finish"
  }
  withName: 'STAR_ALIGN' {
    cpus = 16
    memory = '64.GB'
  }
}

 
